<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cancel_Date_between_Effective_and_Expiry</fullName>
    <active>true</active>
    <description>Cancel Date must be between Effective date and Expiry date of a Policy</description>
    <errorConditionFormula>!ISNULL(  Cancel_Date__c ) &amp;&amp;
( Cancel_Date__c   &lt;  Effective_Date__c    || 
 Cancel_Date__c &gt;  Expiration_Date__c )</errorConditionFormula>
    <errorDisplayField>Cancel_Date__c</errorDisplayField>
    <errorMessage>Cancel date must not be less than Policy Effective Date And/Or must not exceed Policy Expiration date!</errorMessage>
</ValidationRule>
