<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Expiration_Year_Percentage__c</fullName>
    <description>Prorated portion of expiration year</description>
    <externalId>false</externalId>
    <formula>IF( ISBLANK( Policy_Cancel_Year__c), 
  (Expiration_Date__c - DATE(YEAR(Expiration_Date__c), 1, 1))/365
  , IF (Policy_Cancel_Year__c == Policy_Effective_Year__c
    , 0
    , (Cancel_Date__c - DATE(YEAR(Expiration_Date__c), 1, 1))/365
  )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Expiration Year Percentage</label>
    <precision>18</precision>
    <required>false</required>
    <scale>4</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
